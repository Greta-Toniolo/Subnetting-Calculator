Before run the script, you have to install the dependences.
Run this command in the terminal:
	pip install -r dependences.txt


Once we got all the dependences, run the script with:
	python subnetting.py


